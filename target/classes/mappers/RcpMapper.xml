<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
  <mapper namespace="Rcp">
  <insert id="insertRcp" parameterType="RCP">
  		insert into RCP
  		(RCP_PK, RCP_TITLE, RCP_CONTENT, RCP_CT, RCP_VEGE, RCP_COUNT, RCP_REG, RCP_HIT, RCP_LIKE, RCP_COM, USR_ID)
  		VALUES(#{RCP_PK},#{RCP_TITLE}, #{RCP_CONTENT}, #{RCP_CT}, #{RCP_VEGE}, #{RCP_COUNT}, sysdate, 0, 0, 0, #{USR_ID})
  </insert>
  
  <insert id="insertResor" parameterType="RCP">
  		insert into RCP_RESO
  		(RCPRS_PK, RCPRS_TITLE, RCPRS_AMOUNT, RCP_PK)
  		VALUES(RCPRS_PK.NEXTVAL, #{RCPRS_TITLE}, #{RCPRS_AMOUNT}, #{RCP_PK})
  </insert>
  
  <insert id="insertCont" parameterType="RCP">
  		insert into RCP_CONT
  		(RCPCT_PK,RCPCT_NUM, RCPCT_CONTENT, RCP_PK)
  		VALUES(RCPCT_PK.NEXTVAL,#{RCPCT_NUM}, #{RCPCT_CONTENT}, #{RCP_PK})
  </insert>
  
  <select id="detailRcp" parameterType="RCP" resultType="RCP">
 	SELECT *
 	FROM RCP
 	WHERE RCP_PK = ${RCP_PK}
  </select>
  <select id="detailRcpTitle" parameterType="RCP" resultType="RCP">
  	SELECT *
  	FROM RCP_RESO
  	WHERE RCP_PK = ${RCP_PK}
  </select>
  <select id="detailRcpCont" parameterType="RCP" resultType="RCP">
  	SELECT *
  	FROM RCP_CONT
  	WHERE RCP_PK = ${RCP_PK}
  </select>
  
  <select id="getPK" resultType="int">
  select RCP_PK.nextval from dual
  </select>
  
<select id="likeRcp" parameterType="RCP">
	SLELCT * 
	FROM (
	FROM RCP 
	ORDER BY RCP_LIKE DESC
	)WHERE ROWNUM<![CDATA[<]]>17
</select>

<select id="countRcp" parameterType="RCP" resultType="int">
SELECT COUNT(*) FROM RCP
</select>
<select id="selectRcp" parameterType="RCP" resultType="RCP">
	SELECT * FROM RCP ORDER BY RCP_PK DESC OFFSET #{start} ROWS FETCH NEXT #{cntPerPage} ROWS ONLY
</select>

<delete id="delcheck">
	DELETE FROM RCP
	WHERE RCP_PK = ${RCP_PK}
</delete>
	
<update id="rewcheck">
	
</update>

  </mapper>