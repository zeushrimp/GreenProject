<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
  <mapper namespace="Adm">
  
    <!-- 회원 목록 보기 (관리자) -->
  	<select id="showAllus" resultType="USR">
  		SELECT *
  		FROM USR
  	</select>
  	
  	 <!-- 회원 정보 수정 (관리자) -->
 	<update id="changeUsrInfo" parameterType="USR">
 		UPDATE USR SET
 		USR_NAME= #{USR_NAME},
 		USR_EMAIL=#{USR_EMAIL},
 		USR_PW=#{USR_PW},
 		USR_ADDR1=#{USR_ADDR1},
 		USR_ADDR2=#{USR_ADDR2},
 		USR_ADDR3=#{USR_ADDR3},
 		USR_TEL=#{USR_TEL}
 		WHERE USR_ID=#{USR_ID}
 	</update>
  	
  	 <!-- 회원 정보 삭제 (관리자) -->
  	 <delete id="delete_usr_info" parameterType="USR">
		DELETE FROM USR
		WHERE
		USR_ID=#{USR_ID} AND
 		USR_PW=#{USR_PW}
 	</delete>
  	
  	
  	
  	
  </mapper>